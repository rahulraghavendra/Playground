#include<iostream>
using namespace std;
int main()
{
 string s1,s2;
  cin>>s1;
  cin>>s2;
  if(s1.compare(s2))
  {
    cout<<"Strings are not same";//Type your code here.
}
  else
    cout<<"Strings are same";
}