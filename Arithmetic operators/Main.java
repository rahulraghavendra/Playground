#include<iostream>
using namespace std;
int main()
{
  int a,b;
  cin>>a>>b;
  int sum,diff,mul,mod,div;
  cout<<(sum=a+b)<<"\n";
  cout<<(diff=a-b)<<"\n";
  cout<<(mul=a*b)<<"\n";
  cout<<(mod=a%b)<<"\n";
  cout<<(div=a/b);
  //Type your code here.
}